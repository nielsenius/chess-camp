<div class="row">
  <div class="small-7 columns">
    <!-- MAIN CONTENT GOES HERE -->
		<h2>User Dashboard</h2><br />
    <%= render partial: "dashboard_instructor" %>
		<%= render partial: "all_upcoming_camps" if current_user.role == "admin" %>
  </div>

  <div class="small-1 columns">
    &nbsp;
  </div>

  <div class="small-4 columns">
    <!-- SIDEBAR CONTENT GOES HERE -->
		<%= render partial: "dashboard_my_camps" %>
		<%= render partial: "report_generator" if current_user.role == "admin" %>
  </div>
</div>
